# Archive

Historical documentation. Nothing here describes the current state of this project. Read it
for why a decision was made; never for what is true now.

## Archived

Nothing yet. `docs/public-deployment/` is the only finished work in the tree, and it stays
live while `spec.md`'s `Deferred questions` register still has open entries — see the
2026-08-22 run record.

## Runs

- `cleanup-2026-08-22.md` — 5 documents in scope: 3 live, 1 corrected, 0 archived, 0 deleted.
