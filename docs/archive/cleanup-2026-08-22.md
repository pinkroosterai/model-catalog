# Docs cleanup 2026-08-22

**Status** — applied 2026-08-22
**Scope** — repo root; 5 documents (`CLAUDE.md`, `README.md`, and the `docs/public-deployment/`
work folder)
**Link baseline** — 1 relative link checked, 0 unresolved

| Document | Verdict | Tier | Evidence | Action |
|---|---|---|---|---|
| `CLAUDE.md` | live | b | every path, symbol, command and CI claim it makes was checked against the repo and holds — see below | none |
| `README.md` | stale | b | `GET /metrics` and `POST /v1/refresh` both return **404** on the public endpoint the README documents, while the table says `open` and `api-key` | **applied** — the two rows now say so |
| `docs/public-deployment/plan.md` | finished | c | plan `Status — done`, all four phases `done`; `92489e0`, `5d5471c`, `a639971`, `991e4d5` land the work, and the service answers 200 on `models.pinkrooster.nl` | **held** — see *Blocked* |
| `docs/public-deployment/spec.md` | live | — | `Deferred questions` register still carries live entries; protected from demotion | none |
| `docs/public-deployment/research.md` | live | — | dated findings; moves with its folder, and the folder is not moving | none |

## What was checked, and held

`CLAUDE.md` — `ModelCatalog.slnx`, `.config/dotnet-tools.json`, `src/ModelCatalog.Client/Dtos/`
and `Aliases/alias-map.json` all exist; `SyncPipeline`, `AliasResolver`, `PriorityMerger`,
`SnapshotStore.TryLoadFromDiskAsync`, `SyncJob`, `RefreshEndpoints`, `MergeOptions`,
`TestAppFactory` and `FakeSource` all resolve; no `launchSettings.json`, so the `:5000` note is
right; cache keys really are `modelregistry:v1:*`; `ci.yml` pushes
`ghcr.io/pinkroosterai/modelcatalog:<sha>` plus `:current` and `release.yml` publishes NuGet only,
exactly as the Release section says. Nothing to correct.

It does not mention `compose.yml`, `.env.example` or the observability contract added in this
work. That is a completeness question, not a truth one — `claude-md-improver`'s, not this run's.

`README.md` — image name, `net10.0`, the `0.2.0` version, the config-key table, the snapshot path
in `deploy/Dockerfile`, the daily-01:00-UTC schedule and the "live since 2026-08-22" claim were
all checked and all hold. `/health`, `/healthz`, `/v1/models`, `/v1/meta` and `/v1/sources` answer
200 publicly. Only the two rows below do not.

## The `README.md` correction

Verified 2026-08-22 against the live service:

```
GET  /metrics      -> 404
POST /v1/refresh   -> 404
```

Both are deliberate — `spec.md § Decisions` records `/metrics` being 404'd at the edge, following
`wa`, `assistant` and `mail`, and refresh likewise. The endpoints exist; they are simply not on
the public door the README sends readers to. Proposed edit, two table cells only:

| Endpoint | Auth | Description |
|---|---|---|
| `GET /metrics` | open | Prometheus metrics. **Not exposed on `models.pinkrooster.nl`** — 404 at the edge; available when self-hosting |
| `POST /v1/refresh` | api-key | Operator-triggered manual sync. **Not exposed on `models.pinkrooster.nl`** — 404 at the edge; available when self-hosting |

Nothing else in the file changes.

## Blocked — the work folder cannot move whole

`plan.md` is `finished` on its own evidence and would normally take its folder to
`docs/archive/public-deployment/` intact. It cannot, because `spec.md` is protected: a spec whose
`Deferred questions` register still has live entries is never demoted, since archiving it hides
the questions. At least three entries are live:

- **`ACME_EMAIL` on the Caddy stack** — `plan.md § Log` states outright that it was raised and
  left deferred.
- **Collector target discovery** — nothing decided; an estate-wide question.
- **A shared `najs-logging` package** — waits on a second consumer, which does not exist yet.

One entry is already dead: *"Should the README's claims be corrected in this work or after it?"*
was answered by `23ac7c0`. One is arguable: the `model_registry_` prefix question is effectively
settled by `CLAUDE.md`'s "Don't 'fix' this", but that answer was never written back into the
register. Neither is proposed for edit here — moving an entry from `Deferred questions` to
`Decisions` is restructuring a document `shape-spec` owns, not correcting a checkable fact.

Splitting the plan out alone is the failure this rule exists to prevent: `plan.md` cites
`research.md § The bind mount would silently fail to persist` and `spec.md § Success criteria` as
siblings, and both citations break the moment it moves.

**The question for you:** resolve the register first with `/resolve-open-questions`, then archive
the folder whole on a later run — or archive it now and accept that five deferred questions go
with it. Recommendation: resolve first. Two of the three live entries are one decision each.

## Unresolved

None.

## Link repairs outside docs

None — nothing moved.

## Not applied

`docs/public-deployment/` — not archived, for the reason under *Blocked*. Confirmed by
the user on 2026-08-22: resolve the register first, archive the folder on a later run.
